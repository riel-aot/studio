'use client';

// This page has been functionally removed per user request.
// The route remains to avoid 404s if accessed directly, but renders nothing.
export default function RubricsPage() {
  return null;
}
